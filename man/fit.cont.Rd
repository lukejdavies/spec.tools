\name{fit.cont}
\alias{fit.cont}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{
Function to fit the continum of a spectrum using multipule interations of a 4th order polynomial. 
}
\description{
Fits the spectral continuum using an iterative combination of 4th order polynomials. Similar to fit.cont.order but allows only 4th order polynomials.
}
\usage{
fit.cont(spec, iter)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{spec}{
List with the format generated by get.spec
}
  \item{iter}{
Number of iterations to perform
}

}
\details{
%%  ~~ If necessary, more details than the description above ~~
}
\value{
\item{mod }{List with the same format as input spec, but where mod$flux is the spectral continuum fit.}

}
\references{
%% ~put references to the literature/web site here ~
}
\author{
Luke Davies
}
\note{
%%  ~~further notes~~
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
%% ~~objects to See Also as \code{\link{help}}, ~~~
}
\examples{
spec<-get.spec(paste(.libPaths(),'/fourXPS/data/ExampleSpec.fits',sep=''))

spec.plot(spec, main='Example Spectrum', xlim=c(4500,9000), col='navy', lwd=0.5)

mod<-fit.cont(spec, 4)

spec.plot(mod,col='red', oplot=T)

}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
\keyword{ }
\keyword{ }% __ONLY ONE__ keyword per line
